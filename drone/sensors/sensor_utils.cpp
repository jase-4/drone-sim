// sensor_utils.cpp
#include "sensor_utils.hpp"
#include <random>
#include <glm/glm.hpp>

// std::random_device SensorUtils::rd;
// std::mt19937 SensorUtils::gen(SensorUtils::rd());

// glm::vec3 SensorUtils::generateRandomNoise(float noiseLevel) {
//     std::uniform_real_distribution<float> dist(-noiseLevel, noiseLevel);
//     return glm::vec3(dist(gen), dist(gen), dist(gen));
// }

// float SensorUtils::generateRandomNoiseFloat(float noiseLevel) {
//     std::uniform_real_distribution<float> dist(-noiseLevel, noiseLevel);
//     return dist(gen);
// }
